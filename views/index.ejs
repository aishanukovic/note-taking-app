<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Note-Taking App</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="/css/style.css" rel="stylesheet">
    </head>
    <body>
        <%- include('partials/header.ejs') %>
        
        <div class="container mt-5">
            <div class="col-md-12 text-center">
                <h1>Welcome to our simple and easy-to-use Note-Taking App!</h1>
                <p>Keep your thoughts organized with our app. Sign in or register to get started.</p>

                <% if (typeof user === 'undefined' || !user) { %>
                    <a href="/auth/login" class="btn btn-primary">Login</a>
                    <a href="/auth/register" class="btn btn-success">Register</a>
                <% } else { %>
                    <a href="/user/homepage" class="btn btn-info">Go to Homepage</a>
                <% } %>

            </div>
        </div>

        <%- include('partials/footer.ejs') %>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>